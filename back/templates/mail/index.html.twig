{% extends 'base.html.twig' %}

{% block title %}Mail index{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="{{ asset('js/script.js') }}"></script>
{% endblock %}

{% block body %}
    <h1>Mail index</h1>

    <table class="table" id="myTable">
        <thead>
            <tr>
                <th>Id</th>
                <th>Objet</th>
                <th>Text</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mail in mails %}
            <tr>
                <td>{{ mail.id }}</td>
                <td>{{ mail.objet }}</td>
                <td>{{ mail.text }}</td>
                <td>
                    <a href="{{ path('app_mail_show', {'id': mail.id}) }}" class="btn btn-info">show</a>
                    <a href="{{ path('app_mail_edit', {'id': mail.id}) }}" class="btn btn-warning">edit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_mail_new') }}" class="btn btn-primary">Créer un nouveau</a>
{% endblock %}
